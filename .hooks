#!/bin/bash

# Hooks
local quit="y"
local cmd=""
local cmd_alias=""

# File
local file="package.json"

zsh_add_source ~/.dotfiles/hooks/pos_validation
zsh_add_source ~/.dotfiles/hooks/pre_validation

autoload -U add-zsh-hook                  # Load the zsh hook module
