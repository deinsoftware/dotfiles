#!/bin/zsh

params=""
if [[ ! -z $@ ]]; then
    params="$@"
fi

echo "${T_GREEN}❯ ${(P)PACKAGE} ${(P)${:-${PACKAGE}_UNINSTALL}} ${params}" # Print it
${(P)PACKAGE} ${(P)${:-${PACKAGE}_UNINSTALL}} ${params}