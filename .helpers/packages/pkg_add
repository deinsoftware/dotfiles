#!/bin/zsh

params=""
if [[ ! -z $@ ]]; then
    params="$@"
fi

echo "${T_GREEN}❯ ${T_YELLOW}${(P)PACKAGE} ${(P)${:-${PACKAGE}_ADD}} ${params}${F_RESET}" # Print it
${(P)PACKAGE} ${(P)${:-${PACKAGE}_ADD}} ${params}